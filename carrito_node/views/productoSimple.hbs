
<div class="container mb-4">
    <div class="row ">
        <div class="col-md-5 text-center">
            <img src="/images/{{prodSingle.imagen}}">
        </div>

        <div class="col-md-6">
            <h2 class="mb-3">{{prodSingle.nombre}}</h2>            
            <p class="textos">{{prodSingle.descripcion}}</p>
            <p class="stock">Stock: {{prodSingle.stock}}</p>
            <p class="precio">${{prodSingle.precio}}</p>     
        </div>
    </div>
</div>

     
     
